function solution(num1, num2) {
    // 방법1
    var answer = 0;
    answer = (num1 === num2) ? 1 : -1;
    return answer;
    
    // 방법2
    // return (num1 === num2) ? 1 : -1;
    
    // 방법3
    // if(num1 === num2) {
    //   answer = 1;
    // } else {
    //   answer = -1
    // }
    // return answer;
}